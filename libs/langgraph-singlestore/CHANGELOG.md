# Changelog

All notable changes to `langgraph-singlestore` are documented in this file.

## [Unreleased]

- Initial scaffolding: `SingleStoreSaver`, `AsyncSingleStoreSaver`, and
  `SingleStoreStore` placeholders under the `langgraph.checkpoint.singlestore`
  and `langgraph.store.singlestore` namespace packages.
